<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.F1App"
        tools:targetApi="31">
        <activity
            android:name=".activities.SplachActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.DriverinfoActivity" />
        <activity android:name=".activities.ConstructorinfoActivity" />
        <activity android:name=".activities.PastRaceInfosActivity"/>
        <activity android:name=".activities.UpcomingRaceInfosActivity"/>
        <activity android:name=".activities.PastRaceResultActivity"/>
        <activity android:name=".activities.UpcomingRaceResultActivity"/>
        <activity android:name=".activities.LatestinfoActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>